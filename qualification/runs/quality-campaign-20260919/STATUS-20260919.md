# Quality campaign status — paused 2026-09-19 ~10:15

The owner stopped the campaign mid-run so the GPU could be used for other
work. It will be re-run in full tonight. Raw results from these passes are
local only; they are git-ignored (`results/`, `status.json`, `first-pass/` …
`fourth-pass/`, `baselines/`).

## What ran

| Phase | State when paused |
|---|---|
| Smoke (18 stages) | 5 passes. Pass 5 plus reruns: **7 stages fully clean** (Qwen3.6 ×3, Qwen3.8 ×2, Flash-Next ×2). The rest fail only on documented model behaviour, per `PASS-5-TRIAGE.md`. Laguna was confirmed after its fix (see below). |
| 20×20 sanity (17 of 18; Laguna held) | **12 passed**: Qwen3.6 ×3, Qwen3.8 ×2, Flash-Next ×2, Muse ×3, North ×2. **Xing ordinary 379/400 and Xing MTP-1 377/400 failed.** Both failures were empty replies, a harness thinking-allowance bug now fixed in `b490d0f`. The run was stopped at round ~10/20 of Xing prompt lookup, which had four 503s (untriaged). Gemma 3n and MiniCPM-o were not run. |
| Context ladder | Not started. |

## Product fixes found by this campaign (all on this branch, CPU-tested)

- `9f0398e`: external-draft (DFlash2) insertion rejected ordinary serving metadata, so every Muse DFlash2 request failed. **GPU-confirmed** (Muse DFlash2 went from 1 to 28+ passing core checks).
- `6d4e548`: Gemma 3n output leaked `▁` because the wrong detokenizer was used. **GPU-confirmed.**
- `30c3a5d`: Flash-Next MTP2 crashed with `OrderedDict mutated during iteration` in diagnostics. **GPU-confirmed** (the stage passed).
- `45d9b98` + `860a600`: structured output treated an adapter's end-of-turn token (Laguna `</assistant>`) as grammar text, because the next-token row is evaluated before stop matching. The fix adds one canonical stop-token set for all routes. Main's behaviour for a stop inside thinking is preserved. **GPU-confirmed**: in a brief check at `b490d0f`, Laguna core went from 35/4 to 39/0, and Qwen3.6 MTP2 stayed at 40/0.
- `edc528b`: bounded structured-output dead-end diagnostics, emitted in qualification mode only.
- `bd67938`: the dry run validates server arguments with the real pre-load engine checks.

Harness fixes are listed in the commit log and in `PASS-3/4/5-TRIAGE.md`.

## For tonight's full rerun

- Serve from a pinned clean copy of the branch tip (`MLX2_CAMPAIGN_ROOT`), under the `cpg_job` lease, one phase at a time. Pass stage names as separate arguments: zsh does not word-split an unquoted variable.
- **Known runner issue:** SIGTERM to the runner, or to `cpg_job`, does not kill the stage's server and client, which run in their own process group. Kill them explicitly after stopping. Fix this before tonight if convenient.
- Still to triage: the Xing prompt-lookup 503s under a 20-wide load (probably queue or admission timeouts at width 1).
- Gemma 3n and MiniCPM-o 20×20 are useful mainly as mlx-vlm concurrency and serving-health checks. Grade them on serving health, not text correctness.
- The ladder's own estimate is 36–72 h at full scope. Consider trimming it: width 4 only up to 32K, a 128K cap except on the models where long context matters, and served models only.
